# Pull-request validation bootstrap test

This temporary file exists only to trigger and verify the read-only pull-request validation workflow.

The test must confirm that a pull request targeting `main` runs:

- locked dependency installation
- Astro Check
- production build

This test file will not be merged into `main`.
